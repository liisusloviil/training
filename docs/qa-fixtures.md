# QA Fixtures for Import

Дата: 23 февраля 2026

## Набор эталонных файлов

- `tests/fixtures/import/Training.csv`
  - ожидаемый результат: preview + save проходят успешно;
  - итог: создаётся активный план `Training`.
  - особенности: маркеры `неделя N` и дни недели в первой колонке, формат `N×A-B`.

- `tests/fixtures/import/invalid-range.csv`
  - ожидаемый результат: бизнес-ошибка валидации диапазона повторов;
  - итог: сохранение не выполняется, страница остаётся рабочей.

## Где используются

- manual QA (`TESTING_STAGE_8.md`, TC-M02);
- e2e smoke (`tests/e2e/mvp-flow.spec.ts`);
- локальная регрессия импорт-потока.

## Правило воспроизводимости

Для всех проверок импорта использовать только эти файлы, чтобы результаты были сопоставимы между разработкой, QA и CI.
